# Direct imports across the codebase
requests==2.32.3
pandas==2.2.3
numpy==2.1.3
SQLAlchemy==2.0.45
psycopg2-binary==2.9.10        # DBAPI driver SQLAlchemy uses for postgresql:// URLs

# HTML / XML scraping
beautifulsoup4==4.14.2
lxml==5.4.0                    # backend for pd.read_html (cy_06) and bs4 fast parser

# PDF extraction
pdfplumber==0.11.8
PyMuPDF==1.27.1                # imported as `fitz` in residence-permits + LRO pipelines

# Excel readers / writers
openpyxl==3.1.5                # .xlsx read+write
xlrd==2.0.2                    # required by pandas for legacy .xls (BIFF) files from ELSTAT

# Pretty-printing
tabulate==0.9.0                # used by pandas .to_markdown() in dashboard/reports
